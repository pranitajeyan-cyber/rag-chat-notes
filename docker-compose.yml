services:
  rag-chat:
    build: .
    container_name: rag-chat-notes
    ports:
      - "8501:8501"
    volumes:
      - ./docs:/app/docs
      - ./chroma_db:/app/chroma_db
      - ./.env:/app/.env
    restart: unless-stopped
